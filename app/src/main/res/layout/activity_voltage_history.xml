<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://android.com"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#121212"
    tools:context=".VoltageHistoryActivity">

    <!-- Back Button Header Layer -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btn_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:srcCompat="@drawable/ic_back_arrow"
            app:tint="#FFFFFF"
            android:contentDescription="Go Back"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="VOLTAGE TIMELINE ANALYSIS"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

    <com.github.mikephil.charting.charts.LineChart
        android:id="@+id/voltage_trend_chart"
        android:layout_width="match_parent"
        android:layout_height="380dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/layout_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Stock-Style Interval Filter Bar -->
    <LinearLayout
        android:id="@+id/layout_time_filters"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="20dp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/voltage_trend_chart">

        <!-- CHANGED: Swapped text to 1H, 7D, 1Y, ALL -->
        <Button android:id="@+id/btn_filter_1d" android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1" android:text="1H" android:textSize="11sp" android:textColor="#FFFFFF" android:backgroundTint="#222222" style="?android:attr/borderlessButtonStyle"/>
        <Button android:id="@+id/btn_filter_7d" android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1" android:text="7D" android:textSize="11sp" android:textColor="#666666" android:backgroundTint="#121212" style="?android:attr/borderlessButtonStyle"/>
        <Button android:id="@+id/btn_filter_1m" android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1" android:text="1Y" android:textSize="11sp" android:textColor="#666666" android:backgroundTint="#121212" style="?android:attr/borderlessButtonStyle"/>
        <Button android:id="@+id/btn_filter_1y" android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1" android:text="All" android:textSize="11sp" android:textColor="#666666" android:backgroundTint="#121212" style="?android:attr/borderlessButtonStyle"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
